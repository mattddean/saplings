/**
 * The entries that this application considers valid in the name column of UserPetitionRole.
 * This is necessary because we've chosen to represent roles as a table rather than a database enum.
 */
export enum UserPetitionRoleName {
  ADMIN = "admin",
}
